# Kotlin
kotlin.code.style=official
# ?????????? in-process ????????? ? Jenkins (??????????? ??????), ????? ????????? daemon heap ?????????
kotlin.daemon.jvmargs=-Xmx1536M

# Gradle JVM ????? (???????????? ? CI):
# - ??????? Xmx ????? ???????? ???????? ?? OOM, ???????? heap dump ? ???? ??????
# - ???????????? metaspace, ????? GC
org.gradle.jvmargs=-Xms256m -Xmx2048m -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError -XX:ErrorFile=hs_err_pid%p.log -Dfile.encoding=UTF-8 -XX:+UseParallelGC -XX:+ExitOnOutOfMemoryError

# ????????? daemon ? ????????? ??? ??????????????? ? ???????????? CI ????????
org.gradle.daemon=false
org.gradle.parallel=false
org.gradle.workers.max=1

# ????????? configuration cache (???? ???????? ??????? ? KMP/Compose ??? ???????????? ?????????????)
org.gradle.configuration-cache=false

# ???????? build cache (???????) ? ????????? ??????????
org.gradle.caching=true

# Android
android.nonTransitiveRClass=true
android.useAndroidX=true

# Kotlin incremental
kotlin.incremental=true
